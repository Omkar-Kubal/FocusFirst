<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service descriptor for Focus Guard.

    android:accessibilityEventTypes  — only care about foreground app changes
    android:accessibilityFeedbackType — generic (non-audio, non-haptic)
    android:accessibilityFlags        — include views not marked important
    android:canRetrieveWindowContent  — needed to read package name reliably
    android:description               — shown in the Accessibility settings UI
    android:settingsActivity          — "Settings" button in Accessibility UI
                                        routes to FocusGuardScreen (embedded in main)
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:description="@string/focus_guard_description"
    android:notificationTimeout="100" />
